import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  button: {
    backgroundColor: '#46daff'
  },
  inputContainer: {
    backgroundColor: 'white',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center'
  },
  input: {
    color: 'black',
    height: 40,
    width: '60%',
    marginTop: 12,
    marginBottom: 12,
    borderWidth: 1,
    padding: 10
  }
})

export default styles
